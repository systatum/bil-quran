[project]
name = "shared"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SeeStarz", email = "mfahrim7@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
shared = "shared:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
