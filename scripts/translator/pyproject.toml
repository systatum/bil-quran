[project]
name = "translator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "SeeStarz", email = "mfahrim7@gmail.com" }]
requires-python = ">=3.11"

[tool.uv.sources]
shared = { workspace = true }

[tool.uv.workspace]
members = ["backend", "frontend", "proxy", "shared"]
