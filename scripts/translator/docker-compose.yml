services:
  translator:
    image: bil-quran/translator
    container_name: bil-quran_translator
    build: .
    volumes:
      - ./models:/app/models:rw
      - ./hf_cache:/app/hf_cache:rw
    ports:
      - "8000:8000"
    gpus: all
    environment:
      - HF_HOME=/app/hf_cache
